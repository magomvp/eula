{% extends '::base.html.twig' %}

{% block body -%}
    <h1>NewsletterCampaign list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Subject</th>
                <th>Name</th>
                <th>Fromuser</th>
                <th>Touser</th>
                <th>Content</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('newslettercampaign_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.subject }}</td>
                <td>{{ entity.name }}</td>
                <td>{{ entity.fromUser }}</td>
                <td>{{ entity.toUser }}</td>
                <td>{{ entity.content }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('newslettercampaign_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('newslettercampaign_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('newslettercampaign_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
